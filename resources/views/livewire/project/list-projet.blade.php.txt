<div class="space-y-6">
    {{-- Header avec statistiques --}}
    <div class="bg-white rounded-lg shadow p-6">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 mb-6">
            <h2 class="text-3xl font-bold text-black">Projets</h2>
            <div class="flex flex-wrap gap-3 text-sm">
                <div class="bg-primary/10 px-4 py-2 rounded-full border border-primary/20">
                    <span class="text-primary font-bold">{{ $stats['total'] }}</span>
                    <span class="text-primary/80 ml-1">Total</span>
                </div>
                <div class="bg-green-50 px-4 py-2 rounded-full border border-green-200">
                    <span class="text-green-600 font-bold">{{ $stats['published'] }}</span>
                    <span class="text-green-500 ml-1">Publiés</span>
                </div>
                <div class="bg-secondary/10 px-4 py-2 rounded-full border border-secondary/20">
                    <span class="text-secondary font-bold">{{ $stats['featured'] }}</span>
                    <span class="text-secondary/80 ml-1">En vedette</span>
                </div>
                <div class="bg-yellow-50 px-4 py-2 rounded-full border border-yellow-200">
                    <span class="text-yellow-600 font-bold">{{ $stats['in_progress'] }}</span>
                    <span class="text-yellow-500 ml-1">En cours</span>
                </div>
            </div>
        </div>
        
        {{-- Filtres --}}
        <div class="grid grid-cols-1 md:grid-cols-6 gap-4">
            <div class="md:col-span-2">
                <label class="block text-sm font-bold text-gray-700 mb-2">Recherche</label>
                <input
                    type="text"
                    wire:model.live="search"
                    placeholder="Titre, description..."
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary transition-colors"
                />
            </div>
            
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Statut</label>
                <select wire:model.live="statusFilter" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary">
                    <option value="all">Tous</option>
                    @foreach($projectStatuses as $status)
                        <option value="{{ $status['value'] }}">{{ $status['label'] }}</option>
                    @endforeach
                </select>
            </div>
            
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Type</label>
                <select wire:model.live="typeFilter" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary">
                    <option value="all">Tous</option>
                    @foreach($projectTypes as $type)
                        <option value="{{ $type['value'] }}">{{ $type['label'] }}</option>
                    @endforeach
                </select>
            </div>
            
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Priorité</label>
                <select wire:model.live="priorityFilter" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary">
                    <option value="all">Toutes</option>
                    @foreach($priorities as $priority)
                        <option value="{{ $priority['value'] }}">{{ $priority['label'] }}</option>
                    @endforeach
                </select>
            </div>
            
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Trier par</label>
                <select wire:model.live="sortBy" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary">
                    <option value="created_at">Date de création</option>
                    <option value="title">Titre</option>
                    <option value="budget">Budget</option>
                    <option value="deadline">Date limite</option>
                </select>
            </div>
            
            <div class="flex items-end gap-3">
                <label class="flex items-center cursor-pointer">
                    <input type="checkbox" wire:model.live="showFeaturedOnly" class="rounded border-gray-300 text-primary focus:ring-primary">
                    <span class="ml-2 text-sm font-medium text-gray-700">⭐ En vedette</span>
                </label>
                <label class="flex items-center cursor-pointer">
                    <input type="checkbox" wire:model.live="showPublishedOnly" class="rounded border-gray-300 text-primary focus:ring-primary">
                    <span class="ml-2 text-sm font-medium text-gray-700">Publiés</span>
                </label>
            </div>
        </div>
    </div>

    <!-- Liste des projets - Cards horizontales pour md/lg, verticales pour mobile -->
    <div class="space-y-6">
        @forelse($projects as $project)
            <div wire:key="project-{{ $project->id }}" class="bg-white rounded-lg shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden group">
                <!-- Mobile: Layout vertical -->
                <div class="md:hidden">
                    <!-- Image featured -->
                    @if($project->featured_image)
                        <div class="h-48 bg-gray-100 overflow-hidden">
                            <img src="{{ $project->featured_image }}" alt="{{ $project->title }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                        </div>
                    @endif
                    
                    <div class="p-6">
                        <!-- Header -->
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex-1">
                                <div class="flex flex-wrap items-center gap-2 mb-2">
                                    @if($project->is_featured)
                                        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-secondary/10 text-secondary border border-secondary/20">
                                            ⭐ En vedette
                                        </span>
                                    @endif
                                    
                                    @switch($project->status)
                                        @case('pending')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-yellow-100 text-yellow-800 border border-yellow-200">
                                                ⏳ En attente
                                            </span>
                                            @break
                                        @case('in_progress')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-blue-100 text-blue-800 border border-blue-200">
                                                ⚙️ En cours
                                            </span>
                                            @break
                                        @case('completed')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-green-100 text-green-800 border border-green-200">
                                                ✅ Complété
                                            </span>
                                            @break
                                        @case('published')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-purple-100 text-purple-800 border border-purple-200">
                                                📢 Publié
                                            </span>
                                            @break
                                    @endswitch
                                </div>
                                
                                <h3 class="text-xl font-bold text-black mb-1 group-hover:text-primary transition-colors">{{ $project->title }}</h3>
                                <p class="text-sm text-gray-500 font-mono">{{ $project->code }}</p>
                            </div>
                        </div>
                        
                        <!-- Description -->
                        <div class="mb-4">
                            <p class="text-sm text-gray-700 leading-relaxed">{{ Str::limit($project->description, 150) }}</p>
                        </div>
                        
                        <!-- Informations clés -->
                        <div class="grid grid-cols-2 gap-3 mb-4">
                            <div class="bg-gray-50 p-3 rounded-lg">
                                <div class="text-xs text-gray-500 mb-1">Budget</div>
                                <div class="text-lg font-bold text-primary">{{ number_format($project->budget, 0, ',', ' ') }} FCFA</div>
                            </div>
                            <div class="bg-gray-50 p-3 rounded-lg">
                                <div class="text-xs text-gray-500 mb-1">Client</div>
                                <div class="text-lg font-bold text-black">{{ $project->client?->name ?? 'N/A' }}</div>
                            </div>
                            @if($project->deadline)
                                <div class="bg-gray-50 p-3 rounded-lg">
                                    <div class="text-xs text-gray-500 mb-1">Date limite</div>
                                    <div class="text-lg font-bold {{ $project->deadline->isPast() ? 'text-red-600' : 'text-black' }}">
                                        {{ $project->deadline->format('d/m/Y') }}
                                    </div>
                                </div>
                            @endif
                            @if($project->progress_percentage)
                                <div class="bg-gray-50 p-3 rounded-lg">
                                    <div class="text-xs text-gray-500 mb-1">Progression</div>
                                    <div class="text-lg font-bold text-primary">{{ $project->progress_percentage }}%</div>
                                </div>
                            @endif
                        </div>
                        
                        <!-- Barre de progression -->
                        @if($project->progress_percentage)
                            <div class="mb-4">
                                <div class="w-full bg-gray-200 rounded-full h-3">
                                    <div class="bg-gradient-to-r from-primary to-secondary h-3 rounded-full transition-all duration-500" style="width: {{ $project->progress_percentage }}%"></div>
                                </div>
                            </div>
                        @endif
                        
                        <!-- Statistiques sociales -->
                        <div class="flex justify-between items-center text-sm mb-4">
                            <div class="flex items-center">
                                @if($project->average_rating > 0)
                                    <div class="flex items-center">
                                        <span class="text-yellow-400">★</span>
                                        <span class="ml-1 font-bold text-black">{{ number_format($project->average_rating, 1) }}</span>
                                        <span class="text-gray-400 ml-1">({{ $project->reviews_count ?? 0 }})</span>
                                    </div>
                                @else
                                    <span class="text-gray-400">Pas encore noté</span>
                                @endif
                            </div>
                            
                            <div class="flex items-center gap-4">
                                <span class="flex items-center">
                                    <span class="text-red-500">❤️</span>
                                    <span class="ml-1 font-bold text-black">{{ $project->likes_count ?? 0 }}</span>
                                </span>
                                <span class="flex items-center">
                                    <span class="text-blue-500">👁️</span>
                                    <span class="ml-1 font-bold text-black">{{ $project->views_count ?? 0 }}</span>
                                </span>
                            </div>
                        </div>
                        
                        <!-- Technologies -->
                        @if($project->technologies)
                            <div class="mb-4">
                                <div class="flex flex-wrap gap-2">
                                    @php
                                        $technologies = $project->technologies;
                                        $displayedTechs = array_slice($technologies, 0, 3);
                                    @endphp
                                    @foreach($displayedTechs as $tech)
                                        <span class="inline-block px-3 py-1 text-xs font-bold bg-primary/10 text-primary rounded-full border border-primary/20">
                                            {{ $tech }}
                                        </span>
                                    @endforeach
                                    @if(count($technologies) > 3)
                                        <span class="inline-block px-3 py-1 text-xs font-bold bg-gray-100 text-gray-600 rounded-full">
                                            +{{ count($technologies) - 3 }}
                                        </span>
                                    @endif
                                </div>
                            </div>
                        @endif
                        
                        <!-- Actions -->
                        <div class="flex gap-3">
                            <a href="{{ route('projects.detail', $project->slug) }}" class="flex-1 bg-primary text-white text-center px-6 py-3 rounded-lg hover:bg-primary/90 transition-colors font-bold">
                                Voir le projet
                            </a>
                            @if($project->status === 'published')
                                <button class="px-6 py-3 border border-red-600 text-red-600 rounded-lg hover:bg-red-50 transition-colors font-bold">
                                    <span class="text-red-500">❤️</span>
                                </button>
                            @endif
                        </div>
                    </div>
                </div>
              
                <!-- Desktop/Tablet: Layout horizontal -->
                <div class="hidden md:flex">
                    <!-- Colonne gauche: Image et infos principales -->
                    <div class="w-1/3">
                        @if($project->featured_image)
                            <div class="h-full min-h-64 bg-gray-100 overflow-hidden">
                                <img src="{{ $project->featured_image }}" alt="{{ $project->title }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                            </div>
                        @else
                            <div class="h-full min-h-64 bg-gray-100 flex items-center justify-center">
                                <svg class="w-16 h-16 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                </svg>
                            </div>
                        @endif
                    </div>

                    <!-- Colonne milieu: Contenu principal -->
                    <div class="flex-1 p-6">
                        <!-- Header -->
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex-1">
                                <div class="flex flex-wrap items-center gap-2 mb-3">
                                    @if($project->is_featured)
                                        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-secondary/10 text-secondary border border-secondary/20">
                                            ⭐ En vedette
                                        </span>
                                    @endif
                                    
                                    @switch($project->status)
                                        @case('pending')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-yellow-100 text-yellow-800 border border-yellow-200">
                                                ⏳ En attente
                                            </span>
                                            @break
                                        @case('in_progress')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-blue-100 text-blue-800 border border-blue-200">
                                                ⚙️ En cours
                                            </span>
                                            @break
                                        @case('completed')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-green-100 text-green-800 border border-green-200">
                                                ✅ Complété
                                            </span>
                                            @break
                                        @case('published')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-purple-100 text-purple-800 border border-purple-200">
                                                📢 Publié
                                            </span>
                                            @break
                                    @endswitch
                                </div>
                                
                                <h3 class="text-2xl font-bold text-black mb-2 group-hover:text-primary transition-colors">{{ $project->title }}</h3>
                                <p class="text-sm text-gray-500 font-mono mb-4">{{ $project->code }}</p>
                                <p class="text-gray-700 leading-relaxed mb-4">{{ Str::limit($project->description, 200) }}</p>
                            </div>
                        </div>

                        <!-- Technologies -->
                        @if($project->technologies)
                            <div class="mb-4">
                                <div class="flex flex-wrap gap-2">
                                    @php
                                        $technologies = $project->technologies;
                                        $displayedTechs = array_slice($technologies, 0, 5);
                                    @endphp
                                    @foreach($displayedTechs as $tech)
                                        <span class="inline-block px-3 py-1 text-xs font-bold bg-primary/10 text-primary rounded-full border border-primary/20">
                                            {{ $tech }}
                                        </span>
                                    @endforeach
                                    @if(count($technologies) > 5)
                                        <span class="inline-block px-3 py-1 text-xs font-bold bg-gray-100 text-gray-600 rounded-full">
                                            +{{ count($technologies) - 5 }}
                                        </span>
                                    @endif
                                </div>
                            </div>
                        @endif
                    </div>

                    <!-- Colonne droite: Métadonnées et actions -->
                    <div class="w-1/4 bg-gray-50 p-6 border-l border-gray-200">
                        <!-- Informations clés -->
                        <div class="space-y-4 mb-6">
                            <div>
                                <div class="text-xs text-gray-500 mb-1">Budget</div>
                                <div class="text-xl font-bold text-primary">{{ number_format($project->budget, 0, ',', ' ') }} FCFA</div>
                            </div>
                            
                            <div>
                                <div class="text-xs text-gray-500 mb-1">Client</div>
                                <div class="text-lg font-bold text-black">{{ $project->client?->name ?? 'N/A' }}</div>
                            </div>
                            
                            @if($project->deadline)
                                <div>
                                    <div class="text-xs text-gray-500 mb-1">Date limite</div>
                                    <div class="text-lg font-bold {{ $project->deadline->isPast() ? 'text-red-600' : 'text-black' }}">
                                        {{ $project->deadline->format('d/m/Y') }}
                                    </div>
                                </div>
                            @endif
                            
                            <div>
                                <div class="text-xs text-gray-500 mb-1">Type</div>
                                <div class="text-lg font-bold text-black">{{ $project->type->label() }}</div>
                            </div>
                            
                            @if($project->progress_percentage)
                                <div>
                                    <div class="text-xs text-gray-500 mb-2">Progression</div>
                                    <div class="w-full bg-gray-200 rounded-full h-3">
                                        <div class="bg-gradient-to-r from-primary to-secondary h-3 rounded-full transition-all duration-500" style="width: {{ $project->progress_percentage }}%"></div>
                                    </div>
                                    <div class="text-center text-sm font-bold text-primary mt-1">{{ $project->progress_percentage }}%</div>
                                </div>
                            @endif
                        </div>

                        <!-- Statistiques sociales -->
                        <div class="flex items-center justify-between text-sm mb-6 p-3 bg-white rounded-lg">
                            <div class="flex items-center">
                                @if($project->average_rating > 0)
                                    <div class="flex items-center">
                                        <span class="text-yellow-400">★</span>
                                        <span class="ml-1 font-bold text-black">{{ number_format($project->average_rating, 1) }}</span>
                                        <span class="text-gray-400 ml-1">({{ $project->reviews_count ?? 0 }})</span>
                                    </div>
                                @else
                                    <span class="text-gray-400">Pas encore noté</span>
                                @endif
                            </div>
                            
                            <div class="flex items-center gap-3">
                                <span class="flex items-center">
                                    <span class="text-red-500">❤️</span>
                                    <span class="ml-1 font-bold text-black">{{ $project->likes_count ?? 0 }}</span>
                                </span>
                                <span class="flex items-center">
                                    <span class="text-blue-500">👁️</span>
                                    <span class="ml-1 font-bold text-black">{{ $project->views_count ?? 0 }}</span>
                                </span>
                            </div>
                        </div>
                        
                        <!-- Actions -->
                        <div class="space-y-3">
                            <a href="{{ route('projects.detail', $project->slug) }}" class="w-full bg-primary text-white text-center px-4 py-3 rounded-lg hover:bg-primary/90 transition-colors font-bold">
                                Voir le projet
                            </a>
                            @if($project->status === 'published')
                                <button class="w-full px-4 py-3 border border-red-600 text-red-600 rounded-lg hover:bg-red-50 transition-colors font-bold">
                                    <span class="text-red-500">❤️</span> Ajouter aux favoris
                                </button>
                            @endif
                        </div>
                    </div>
                </div>
               
            </div>
        @empty
            <div class="text-center py-16">
                <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
                    <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                    </svg>
                </div>
                <h3 class="text-xl font-bold text-black mb-2">Aucun projet trouvé</h3>
                <p class="text-gray-600 mb-6">Essayez d'ajuster vos filtres de recherche</p>
                <button class="px-6 py-3 bg-primary text-white rounded-lg hover:bg-primary/90 transition-colors font-bold">
                    Réinitialiser les filtres
                </button>
            </div>
        @endforelse
    </div>

    {{-- Pagination --}}
    @if($projects->hasPages())
        <div class="bg-white px-6 py-4 flex items-center justify-between border-t border-gray-200 rounded-lg shadow">
            {{ $projects->links() }}
        </div>
    @endif
</div>